#!/bin/bash
export NGINX_HOST="10.x.x.x"
export NGINX_PORT="80"
export APP_HOST="10.x.x.x"
export APP_PORT="8000"
docker build . -f nginx/Dockerfile --build-arg NGINX_HOST --build-arg NGINX_PORT --build-arg APP_HOST --build-arg APP_PORT -t soylatte_nginx
